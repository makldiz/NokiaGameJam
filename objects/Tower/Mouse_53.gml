/// @description Insert description here
// You can write your code in this editor

//lookingAt = point_direction(x, y, mouse_x, mouse_y);

//bulletSpawnX = x + lengthdir_x(1, lookingAt + gunDirection);
//bulletSpawnY = y + lengthdir_y(gunLength, lookingAt + gunDirection);

//bullet = instance_create_layer(bulletSpawnX, bulletSpawnY, "Instances_1", objBullet);
//bullet.direction = lookingAt;
//bullet.speed = 3;
